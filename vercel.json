{
  "version": 2,
  "buildCommand": "cd \"Nodejs login /frontend\" && npm install && npm run build",
  "outputDirectory": "Nodejs login /frontend/dist",
  "functions": {
    "api/*.js": {
      "runtime": "@vercel/node@3.0.5"
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    }
  ]
}
