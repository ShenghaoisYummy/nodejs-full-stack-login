{
  "name": "nodejs-full-stack-login",
  "version": "1.0.0",
  "description": "Full-stack authentication system with Node.js, Express, and Vite",
  "main": "index.js",
  "scripts": {
    "install:frontend": "cd frontend && npm install",
    "install:backend": "cd backend && npm install",
    "install:database": "cd database && npm install",
    "start:frontend": "cd frontend && npm run dev",
    "start:backend": "cd backend && npm run start",
    "start:database": "cd database && npm run db",
    "dev": "npm run start:database & npm run start:frontend & npm run start:backend",
    "setup": "npm run install:frontend && npm run install:backend && npm run install:database",
    "build": "cd frontend && npm run build",
    "vercel-build": "cd frontend && npm install && npm run build"
  },
  "keywords": ["nodejs", "express", "authentication", "jwt", "bcrypt", "vite", "full-stack"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.5",
    "bcrypt": "^5.1.1",
    "jsonwebtoken": "^9.0.2"
  }
}